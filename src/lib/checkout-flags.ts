/**
 * When false, the “Pay directly (Zelle, Apple Pay…)” option is hidden and card checkout is used.
 * Set to true to show direct payment again (uses short copy only — no account details in UI).
 */
export const SHOW_DIRECT_PAYMENT = false
